# Llaves criptográficas — la PRIVADA nunca entra al repo (regla no negociable)
.keys/
*.key
*.pem
.env
.env.*

# Binarios compilados
software/dsp
software/dsp.exe
*.exe
*.out

# Bases de datos del piloto
*.db
*.db-journal
*.sqlite
*.sqlite3

# QR generados en pruebas locales (los vectores versionados van aparte)
*.png
!especificaciones/**/*.png

# Sistema / editor
.DS_Store
Thumbs.db

*.eml
# Excepción: fixture de test ANONIMIZADO (necesario para `go test ./...`).
# El .eml real con datos personales sigue ignorado por la regla de arriba.
!software/internal/bankmail/testdata/muestra.eml